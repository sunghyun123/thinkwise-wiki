# 실제 값으로 채운 뒤 파일명을 .env로 바꾸세요.
# 반드시 SELECT 권한만 가진 전용 계정을 사용하세요.
DB_HOST=192.168.0.10
DB_PORT=3306
DB_USER=thinkwise_wiki_reader
DB_PASSWORD=CHANGE_ME

# 연결 및 조회가 오래 걸릴 때 애플리케이션 측 연결을 종료하는 시간(초)
DB_CONNECT_TIMEOUT=5
DB_READ_TIMEOUT=10

